# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan*
crash.log
# Secrets / local vars
*.tfvars
*.tfvars.*
!terraform.tfvars.example
# Terraform backend settings (contains the real state storage account name)
backend.hcl
.env
.vscode

# Python (config-sync worker)
__pycache__/
*.pyc
*.pyo

# Admin UI build artifacts
app/admin-ui/spa/node_modules/
app/admin-ui/bff/static/

# Internal one-off scripts / dev tooling — kept on disk, not shipped to customers
internal/

# Local blog drafts
blog-draft-ko.md

# Parallel eastus2 test-stack working dir (operator copy, own state) — not shipped
infra-aigw-eus2/
